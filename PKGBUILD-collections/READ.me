Shuld arledy done building the chroot environment

chroot in the chroot environment

For edit PKGBUILD
refer to build-script of packages in building the chroot environent
mkdir -p /sources/<package-name>/<version>
cp PKGBUILD.skel PKGBUILD
