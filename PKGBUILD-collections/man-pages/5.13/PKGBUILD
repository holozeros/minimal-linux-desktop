pkgname="man-pages"
pkgver="5.13"
pkgrel="1"
pkgdesc=""
arch=('x86_64')
url="http://www."
license=('')
#backup=()
source=(man-pages-5.13.tar.xz)

package() {
    cd "${pkgname}-${pkgver}"
    make DESTDIR=${pkgdir} install
}
